name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  secret-scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Run gitleaks
        uses: gitleaks/gitleaks-action@v2
        with:
          args: --source . --redact

  sca:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: OWASP Dependency-Check
        uses: dependency-check/Dependency-Check_Action@main
        env:
          NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
          JAVA_HOME: /opt/jdk 
        with:
          project: DevSecOpsDevoir
          path: .
          format: "HTML"
          out: dependency-check-report
          args: >-
            --failOnCVSS 7
            --scan gateway
            --scan product-service
            --scan order-service
            --scan react-app

      - name: Upload dependency-check report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dependency-check-report
          path: dependency-check-report

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: react-app/package-lock.json

      - name: npm audit (production)
        run: npm audit --omit=dev --audit-level=high
        working-directory: react-app

  image-scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Build gateway image
        run: docker build -t devsecops/gateway:ci ./gateway

      - name: Build product-service image
        run: docker build -t devsecops/product-service:ci ./product-service

      - name: Build order-service image
        run: docker build -t devsecops/order-service:ci ./order-service

      - name: Build react-app image
        run: docker build -t devsecops/react-app:ci ./react-app

      - name: Trivy scan - gateway
        uses: aquasecurity/trivy-action@0.33.1
        with:
          image-ref: devsecops/gateway:ci
          format: "table"
          exit-code: "1"
          severity: "HIGH,CRITICAL"

      - name: Trivy scan - product-service
        uses: aquasecurity/trivy-action@0.33.1
        with:
          image-ref: devsecops/product-service:ci
          format: "table"
          exit-code: "1"
          severity: "HIGH,CRITICAL"

      - name: Trivy scan - order-service
        uses: aquasecurity/trivy-action@0.33.1
        with:
          image-ref: devsecops/order-service:ci
          format: "table"
          exit-code: "1"
          severity: "HIGH,CRITICAL"

      - name: Trivy scan - react-app
        uses: aquasecurity/trivy-action@0.33.1
        with:
          image-ref: devsecops/react-app:ci
          format: "table"
          exit-code: "1"
          severity: "HIGH,CRITICAL"
          ignore-unfixed: true
          trivyignores: .trivyignore

  sast:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Build gateway
        run: mvn -q -DskipTests package
        working-directory: gateway

      - name: Build product-service
        run: mvn -q -DskipTests package
        working-directory: product-service

      - name: Build order-service
        run: mvn -q -DskipTests package
        working-directory: order-service

      - name: SonarCloud scan
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
